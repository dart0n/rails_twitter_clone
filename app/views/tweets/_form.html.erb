<%= form_with model: current_user.tweets.new, local: true do |f| %>
  <%= render 'shared/error_messages', object: f.object %>

  <div>
    <%= f.text_area :text,  class: 'form-control mb-2' %>
  </div>

  <div>
    <%= f.submit 'Submit', class: 'btn btn-large btn-primary' %>
  </div>
<% end %>