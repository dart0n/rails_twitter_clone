<%= form_with model: current_user.following_relationship.new(followed: user), local: true do |f| %>
  <%= f.hidden_field :followed_id %>
  <%= f.submit "Follow", class: "btn btn-large btn-primary" %>
<% end %>